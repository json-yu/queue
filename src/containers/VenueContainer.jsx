import React from 'react';
import WaitTimesDisplay from '../components/WaitTimeDisplay';

const VenueContainer = (props) => {
  // render map and wait times
  return (
    <div>
      <WaitTimesDisplay />
    </div>
  )
}

export default VenueContainer;